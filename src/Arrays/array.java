package Arrays;

public class array {
    static void ImprimirArray(Double[] array, int tam)
    {int i;
        for(i=0;i<tam;i++)
        {
            System.out.println(array[i] + " ");
        }
    }
}
